# AMD-003 real-boundary pin — reconciliation finding (2026-07-17)

**Run:** `node --env-file=.env scripts/amd003-pin.mjs` against real FuFirE (`https://api.fufire.space`, `x-api-key` auth). The demo Shanghai-1984 profile returned a real `/v1/calculate/zwds` response (pinned at `tests/fixtures/fufire/pinned-real/calculate.json`, untracked pending decision).

**Verdict: DIVERGENCE (pin exit 3).** The real FuFirE contract materially differs from the self-authored golden fixture. The current normalizer would **hard-reject real responses** (`FUFIRE_CONTRACT_MISMATCH`), and AMD-001 fail-closed would refuse them. This is a **CONTRA** between the confirmed value promise CAN-003/VIS-004 ("every explanation traceable to REAL data") and the delivered contract (cannot parse real data).

## Specific divergences (real API vs fabricated fixture)

| Area | Fixture / current contract | REAL FuFirE | Impact |
|---|---|---|---|
| Transformation id | `HUA_QU` (and `HUA_QUAN` is **banned** by `scripts/architecture-gates.mjs`) | **`HUA_QUAN`** | Arch-gate + strict enum reject the REAL id — the canonical-ID assumption is backwards |
| Stars | 16 (MAJOR + YOU_BI, WEN_CHANG) | adds **`ZUO_FU`, `WEN_QU`** (+ more) | `.strict()` star_id enum rejects real stars |
| Top-level | no `catalog` | adds a **`catalog`** object | `.strict()` rejects unknown key |
| `normalized_input.datetime_local` | required string | shaped differently (`invalid_type`) | schema mismatch |
| Palace structure | `palace_id` value set (MING, GUAN_LU, …) | different field/naming (empty on that path) | palace refs don't resolve |
| Ruleset hashes | requires `leap_month/year_cycle/age_reckoning _sha256` | provides `star_catalog/transformation_table/calendar_policy/time_policy _sha256`, **omits the other 3** | schema `invalid_type` on the 3 required-but-absent |
| Ruleset | fixture `ruleset_version 0.3.0`-ish, null hashes | `ruleset_version 0.1.0`, real `ruleset_sha256 3f5cef…`, `source_status SOURCE_NEEDED` | real hashes exist (good) but version/shape differ |

## What this means

- The entire strict contract (`server/normalize.mjs` `rawZwdsSchema` + ID enums in `src/domain/zwdsTypes.ts`), the architecture gate's banned-alias list, and the catalog were built on **guesses that are wrong vs the real engine**. The gate that was supposed to enforce "canonical IDs" actually **bans the real engine's IDs** (`HUA_QUAN`).
- Reconciling to reality is a **re-foundation of the data contract**: re-pin the golden fixture from the real response; rewrite the strict schema to the real shape (catalog object, real `normalized_input`, real palace structure, the hashes the API actually returns); correct the ID enums (`HUA_QUAN`, `ZUO_FU`, `WEN_QU`, real palace ids); fix the architecture gate's banned list; re-derive the catalog; update every test that encoded the fabricated fixture.
- This materially expands slice-1 and is escalated as a **SCOPE SHIFT DECISION** (see `docs/context/decision-log.md` CONTRA-001).

**No public "traceable to real data" claim may ship until this is reconciled** (AMD-003 gate holds, now with concrete evidence).
